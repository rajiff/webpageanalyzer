const worker = require('../api/v1/linkSonar/');
const registerWorker = require('../registerWorker');
const config = require('../config');

registerWorker(config.EVENTS.WEBDOCUMENT_EXTERNAL_LINK_EXTRACTED, worker);